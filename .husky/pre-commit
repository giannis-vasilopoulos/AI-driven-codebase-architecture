if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
else
  echo "gitleaks not installed - skipping local secret scan (CI still enforces it). Install: https://github.com/gitleaks/gitleaks"
fi

pnpm exec lint-staged
